# Exam Integrity System - API Service

This service is built with FastAPI and managed using `uv`.

## Setup & Installation

1. Make sure you have Python 3.11.8 installed.
2. Run `uv sync` to install all pinned dependencies:
   ```bash
   uv sync